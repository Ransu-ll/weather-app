<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="The main webpage of this webapp">
    <meta name="keywords" content="weather, australia, webapp">
    <title>AW2</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='pure.css') }}">
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <h1 class="centre">Aussie's Weather Webapp</h1>
    <h2 class="centre">Aw man, could not find that page!</h2>
    <label for="townsearch">
        <p class="centre">
            Maybe try looking for a town instead?
            <br>
            <input id="townsearch" list="towns">
        </p>
    </label>
    <datalist id="towns">
        {% for town in towns %}
            <option value="{{ town|e }}"></option>
        {% endfor %}
    </datalist>
</body>
</html>